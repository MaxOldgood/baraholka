import cartSlice from './model/cart-slice'
import { CartProductCard } from './ui/cart-product-card'

export { addProduct, incrementCount, decrementCount, deleteProduct } from './model/cart-slice'
export { cartSlice, CartProductCard }
